<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
		
		LISTE DES INSTRUMENT</br>
		<table>
		{% for e in pInstrument %}
		<tr>
			<td><a href="{{ path('instrumentConsulter', { 'id': e.id }) }}">{{ e.nom }}</a></td>
			<td>{{ e.numSerie }}</a></td>
			<td>{{ e.dateAchat|date('d/m/Y') }}</td>
            <td>{{ e.prixAchat }}</a></td>
            <td>{{ e.utilisation }}</a></td>
            <td>{{ e.cheminImage }}</a></td>
			<td>{{ e.couleur}}</td>
	{% else %}
		<tr>
		<td>Aucun instrument n'a été trouvé.</td>
		</tr>
	{% endfor %}
</table>
	
	 
    </body>
</html>