<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
		
		LISTE DES INTERVENTIONS</br>
		<table>
            {% for p in pProfessionnels %}
            <tr>
                <td><a href="{{ path('app_professionnel_consulter', { 'id': p.id }) }}">{{ p.id }}</a></td>
                <td><a href="{{ path('app_professionnel_consulter', { 'id': p.id }) }}">{{ p.nom }}</a></td>
                <td><a href="{{ path('app_professionnel_consulter', { 'id': p.id }) }}">{{ p.ville }}</a></td>
            </tr>
            {% else %}
            <tr>
                <td>Aucun professionnel n'a été trouvé.</td>
            </tr>
            {% endfor %}
        </table>
    </body>
</html>