{% extends 'base.html.twig' %}

{% block title %}Projet Utilisateurs{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Bienvenue sur le projet utilisateurs</h1>
    {% if is_granted('ROLE_ELEVE') %}
    {% else %}
        <a class="bouton-arriere" href="{{ path('app_registration_register') }} ">S'inscrire</a>
        <a class="bouton-arriere" href="{{ path('app_security_login') }} ">Se connecter</a>
    {% endif %}
</div>
{% endblock %}