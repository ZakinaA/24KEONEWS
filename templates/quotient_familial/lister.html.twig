<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
		LISTE DES QUOTIENTS FAMILIAUX</br>
		<table>
            {% for quotient_familial in pQuotient_familiaux %}
            <tr>
                <td><a href="{{ path('app_quotient_familial_consulter', { 'id': quotient_familial.id }) }}">{{ quotient_familial.libelle }}</a></td>
                <td>{{ quotient_familial.quotientMini }}</td>
            </tr>
            {% else %}
            <tr>
                <td>Aucun etudiant n'a été trouvé.</td>
            </tr>
            {% endfor %}
        </table>
    </body>
</html>